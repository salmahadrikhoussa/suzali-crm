/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Make sure there's no custom rewrites or redirects affecting API routes
  // You can add other Next.js config options as needed
};

module.exports = nextConfig;